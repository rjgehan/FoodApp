package com.gehan.mealplanner.domain;

/** Top level of the recipe catalog — the drawer a recipe is filed in. */
public enum RecipeSection {
    BREAKFAST,
    LUNCH,
    DINNER,
    SNACKS,
    DRINKS,
    OTHER
}
