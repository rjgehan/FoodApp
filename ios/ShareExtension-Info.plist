<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>NSExtension</key>
	<dict>
		<key>NSExtensionPointIdentifier</key>
		<string>com.apple.share-services</string>
		<key>NSExtensionPrincipalClass</key>
		<string>$(PRODUCT_MODULE_NAME).ShareViewController</string>
		<key>NSExtensionAttributes</key>
		<dict>
			<!-- Runs in the page first, so a shared page arrives as its text. -->
			<key>NSExtensionJavaScriptPreprocessingFile</key>
			<string>GetPageText</string>
			<!--
			  Both, and the order costs something. With WebURL listed, Safari hands over the
			  address instead of running the preprocessor above — so a shared page arrives as
			  a link rather than as its text. That is worth it now the server can read a link
			  properly: structured data for a recipe site, oEmbed for TikTok. And a link is
			  the only thing apps like TikTok ever offer, so without this the extension never
			  appears in their share sheet at all.
			-->
			<key>NSExtensionActivationRule</key>
			<dict>
				<key>NSExtensionActivationSupportsText</key>
				<true/>
				<key>NSExtensionActivationSupportsWebPageWithMaxCount</key>
				<integer>1</integer>
				<key>NSExtensionActivationSupportsWebURLWithMaxCount</key>
				<integer>1</integer>
			</dict>
		</dict>
	</dict>
</dict>
</plist>
