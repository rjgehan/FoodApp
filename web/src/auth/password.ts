/** Keep in sync with AuthDtos.PASSWORD_MIN / PASSWORD_MAX / PASSWORD_RULE on the server. */
export const PASSWORD_MIN = 8;
export const PASSWORD_MAX = 128;
export const PASSWORD_RULE = 'At least 8 characters.';
