# Copy to secret.yaml, fill in real values, and apply — do NOT commit the filled-in version.
# kubectl apply -f k8s/secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: mealplanner-secrets
  namespace: mealplanner
type: Opaque
stringData:
  DB_USER: mealplanner
  DB_PASSWORD: change-me
  JWT_SECRET: change-me-to-a-long-random-string
